---
name: chinese-erotic-fiction-writing
description: 中文成人向（H）小说创作通用技能——需求勘察流程、通用写作规范、章节结构、批量扩写工程与坑位。不绑定任何固定玩法，玩法/笔触/题材由勘察确定。触发：用户要求创作/扩写/重写 H 向小说、完善大纲、按参考作品调整文风。
---

# 中文成人向（H）小说创作（通用技能）

> 本技能是**通用写文工具**：不绑定任何固定玩法、题材或笔触。每个用户的需求都不一样，**动笔前必须先勘察**（见下），勘察结果决定一切。文中标注「当前用户〔男〕」的内容只是上一个用户的定制沉淀，作为示例参考，**不得默认套用**。

## 触发条件
- 用户要求写/扩写/重写成人向（H）小说
- 用户提供大纲要求"完善后创作"、要求"学习参考作品"
- 用户抱怨"不够爽/不够肉欲/内容太少/环境描写太多/不够长"

## 调用前必须勘察（每个用户都不同，动笔前必做）

不同用户的性癖取向、喜欢的题材、性别不同，写出来的东西可能完全相反。**创作前必须先了解，不能默认套用任何既有偏好**：

1. **性别 / 作者笔触**：使用者是男性还是女性？
   - 男用户 → 通常要**男作者笔触**：动词先行、直接粗粝、肉感沉重、器官直呼名（骚穴/大奶/肉棒）、对白带压迫感
   - 女用户 → 通常要**女作者笔触**：氛围与情感先行、细腻感官铺陈、渐进铺垫、心理描写多、节奏更柔、隐晦词汇容忍度高
   - 拿不准就直接问"你偏好哪种笔触"，不要猜
2. **性癖/玩法取向**：喜欢什么玩法（露出/制服/多人/调教/纯爱/孕期/偷情…）？**本技能不预设任何玩法**——玩法完全由用户指定。直白程度上限？有无雷区（记录禁忌，绝不触碰）？
3. **喜欢的类型**：题材（古风/现代/仙侠/制服）、视角（第一/第三人称）、H 占比、单章字数目标。
4. **风格参照**：有无参考作品或作者？有则用"短语提取法"学习其写法。
5. 勘察结果写入会话记忆，全程遵守；与任何既有偏好/大纲冲突时以勘察结果为准。
6. 本勘察同样适用于**批量扩写/续写已有长篇**——扩写方向（往哪个玩法加戏、加什么段落）同样因人而异。

## 通用写作规范（不依赖具体玩法）

- **身体反应优先**：发烫/战栗/湿透/咬唇忍住/腿软/收缩/喷水/脚趾蜷缩——用身体反应代替直白叙述。
- **高潮要写足**：不能一句带过，写痉挛/弓起/脚尖绷直/穴肉剧烈收缩/喷涌/眼前发白/脱力。
- **声音与液体**：吸"啧啧"、指奸"咕叽咕叽"、操弄"噗嗤噗嗤"、液体"汩汩/顺着大腿根往下淌"。
- **多维描写法**（写法参考，不绑死）：奶子——形状/触感/乳尖变化/揉捏变形/吸吮反应；小穴——阴唇/阴蒂/穴口/淫水/收缩。每场戏至少完整写一轮。
- **器官称谓遵循勘察结果**：男作者笔触直呼名（骚穴/大奶/肉棒/浓精）；女作者笔触可用更柔的词汇。不要默认某一种。
- **环境描写压到最少**：开头两三句交代场景即进入正题，H/身体反应/心理为核心。
- **单章要充实**：字数目标按用户要求（当前用户〔男〕要求 5000-6000 字/章）。收到"加长/充实"指令时必须真正新增内容，禁止复制原文充数（空重写陷阱，见 Pitfalls）。
- **交付格式**：正文 .md，每章标题下直接开写；结尾留钩子。改动只在对话中口头汇报，文件内不写 changelog。

## 玩法定制（勘察后确定）

**本技能不预设玩法**。勘察出玩法后，把它的"铁律/升级轴/章节结构"沉淀为当次会话的写作规范（可写入接续进度.md），例如：

### 示例：悄悄露出玩法（当前用户〔男〕已确立，仅作参考示例）
核心：别人大概有怀疑但从未真正看见，女主在看不见的地方（背身/衣下/帘后/阴影里）自我玩弄，重点渲染身体真实反应。
- 暴露永远差一步：春光一闪/帘影/气味/水声/微颤——永远不到"明晃晃看见"（群戏章节除外）。旁观者只有"怀疑→侧目→窃窃私语→闻味"。
- 每章必有一场"衣下自玩/背身自玩"戏，脸上强装端庄。
- 旁观者的"怀疑"是兴奋燃料：偷听议论、脚步逼近反而更兴奋、既怕被撞见又隐隐盼着。
- 四级升级轴：一瞥之疑 → 侧目之疑 → 闻味之疑 → 心照不宣。
- 完整写法模板见 `references/quiet-exposure-techniques.md`。

## 章节结构

### 通用骨架
场景开场（两三句）→ 铺垫/疑点铺设 → H 主体（2-4 回合，写足）→ 高潮收尾 → 结尾钩子（引出下章/下个场景）。

### 示例：悄悄露出向结构（当前用户〔男〕）
场景开场（环境+装扮细节）→ 疑点铺设（旁人侧目/怀疑/议论）→ 衣下/背身自玩（身体反应重点）→ 被拆穿或近距离接触（H 主体）→ 高潮收尾（多为无声高潮）→ 结尾钩子。

## 大纲量化结构（字数保障法，通用）

单靠手感写不满目标字数时，必须在大纲层就为字数服务（当前用户〔男〕用此法达成 5-6k/章，含番外）：

- **每章拆 3-4 个场景段**，每段 1200-2000 字，拼接即达标。
- **每段写 1-2 个动作支点**（如"衣下揉奶→摸阴蒂→舔手指"），段内写透，不许空转。
- **大纲每章附"细节清单"**：列出本章必须出现的关键词与动作，写正文时逐项勾销防遗漏。
- 典型分法：铺垫/走光 → 自玩（身体反应重点）→ H 主体 → 余韵/钩子。
- 番外同样按用户要求设字数目标。

## 关键工作流（当前用户〔男〕已固化，通用性高）

1. **修订流程**：用户提修改意见后不要在原文上打补丁。正确流程：把建议同步到大纲 → 删旧正文重建 → 从大纲逐章写 → 自审通过再交付。
2. **写完自审**：自查逻辑一致性（章节号对齐、高潮次数与叙述一致），不要急着给半成品。
3. **多会话接力写作**：长篇跨会话创作时，工作区维护 `接续进度.md`（状态速览/每篇字数表/待办/风格规范/验收标准/技巧备忘），每次会话结束更新，下次先读它再接续；每轮 2-3 章；待办按"差距小的优先"排序。

## 批量扩写工程流程（多章节批量扩充，通用）

用户明确批评过「每次都是存在缓存里面重复写入，找点效率更高的方法」——逐章 patch（每章 8-12 次调用）对把十几章短骨架扩到目标字数不可接受。**已验证方案：锚点批量插入脚本**（模板见 `scripts/batch_insert.py`）：

1. 建 `expand.py`：`INS = [(锚点, 插入文本), ...]`，锚点取原文**段落结尾的整句**（20-60 字、全文唯一）
2. 脚本按锚点在文中位置**降序**插入（防位置偏移）；三重幂等：锚点缺失→报告跳过、锚点重复→跳过、插入文本已存在→跳过；可反复运行
3. 每章 8-15 个插入点（自玩细节、旁观议论、身体反应、对白回合、余韵），一次运行完成 2-4 章；每轮跑 `wordcount.py` 验证缺口再补
4. 新内容用 `write_file` 写 `fixN.py`（`r'''...'''` 原始三引号包裹条目），脚本在结构标记 `\n]\n\ndef main():` 处插入条目，**不要**用内容字符串做标记（内容一变就失配）
5. **收尾验证清单**：① wordcount 每章达标；② 全局重复段检查（`body.count(可疑短语)` + 按 `\n\n` 切段找相同段）；③ 章节标题序号完整性（`^### 第N章` 正则连续）；④ 人物称呼一致性抽查；⑤ 脚本 AST 语法通过

**P0 坑（本系列全踩过一遍）**：
- **引号转义**：含大量对白（英文引号）的插入文本若用 patch 写入，模糊匹配会把文本行截断/吞掉。规则：凡含引号的内容一律 write_file 写独立 py 文件再运行；patch 的 old_string 绝不包含带引号的文本；写完用 `python3 -c "import ast; ast.parse(open('expand.py').read())"` 校验语法
- **锚点嵌套**：锚点绝不能取自己之前插入过的文本——插入文本会包含旧锚点句，导致二次插入、段落被切碎、出现重复段。只取**原始正文**锚点；插入文本内不要包含任何锚点句
- **锚点跨章节冲突**：锚点太短（如"她笑了笑"）会跨章节重复被跳过；同句出现在多章时，改用该章独有的前后文做锚点。锚点造好后先用 `body.count(锚点)` 验证唯一再批量跑
- **锚点漂移**：手工清理重复段后，幂等检查 `text in content` 会失效，旧条目重新插入旧内容——清理正文后必须同步删除 expand.py 里对应的失效条目
- **错记原文**：锚点写错一两个字即失配——锚点造好后先用 `content.find` 验证再批量跑
- **清理重复段**：正文重复段用 Python 脚本精确替换（old 串含 `\n\n` 结构锚定段落边界），不要用 patch；发现重复区（2-3 层交织错位）时用起始/结束关键词定位整块，整体替换为合并后的干净版本，再复查 count 归 1
- **patch 安全**：绝对禁止大块 old_string（超过半章）做 patch 替换；patch 只用在小范围插入（≤200 字），且先确认 old_string 唯一
- **番外字数口径**：`wordcount.py` 的 SHORT 阈值若只匹配正传标准，番外按用户设定目标判断，别误判去硬扩

## 批量参考作品学习（短语提取法，通用）

用户要求"读 N 本/看 N 篇"时，用**短语提取法**替代通读——从每本书 grep 出描写短语片段，快速积累比喻库和动词库：

```bash
find 书库目录 -name "*.txt" | shuf -n 50 > /tmp/books50.txt
while read f; do
  iconv -f GBK -t UTF-8 "$f" 2>/dev/null | grep -oE "关键词[^，。]{2,20}" | head -5
done < /tmp/books50.txt
```
`shuf -n N` 随机采样 + `grep -oE` 提取"词+短语"模式，一轮命令扫完。学完必须提炼成自己的规则，不要照抄具体句子。

## 人物声音统一（当前用户〔男〕示例）

- 女主对白带自贬词："贱""骚""奶子""小穴"
- 男主对白带占有欲与嘲弄："本王的床上养了头小骚猪""叫老爷"
- **其他用户按勘察结果重定人物声线**。

## 参考作品学习（通用）

用户若有本地参考书库，注意其编码（如 GBK 编码的书库直接 read_file 是乱码，需 `iconv -f GBK -t UTF-8` 转码），然后用上文"批量参考作品学习（短语提取法）"的方法学习。本技能自带的高浓度写法提炼见 `references/carnal-writing-techniques.md`；「悄悄露出」玩法模板见 `references/quiet-exposure-techniques.md`（后者为当前用户〔男〕示例玩法，其他用户可另建自己的玩法模板）。

## Pitfalls（通用）

- **技能本身不绑定任何用户偏好（P0 设计原则）**：本技能曾被误做成"绑定悄悄露出玩法+男作家笔触"的专用技能，用户两次明确纠正——"我只是想要一个写文的skill,而不是固定玩法的skill"、"不同人的要求可能不一样，调用skill时候先了解性癖和喜欢的类型，使用者的性别可能会导致喜欢的笔触不一样"。**任何玩法、笔触、字数目标都必须是勘察产物**；技能文件只保留通用方法论，具体用户的定制（玩法铁律、章节结构变体、人物声线）沉淀到当次会话的接续进度.md 或标注「当前用户示例」，不得写死进 SKILL.md 主体。收到"帮我写文"类请求时先勘察再动笔，不要沿用上一本书的口味。同类技能若发现重复（如另建了一个只讲批量扩写的窄技能），应合并进本技能而非保留两个。
- 不要写完就停：高潮不能一句带过，要写具体反应。
- 大纲与正文章节号必须对齐；合并大纲两章会破坏结构。
- 环境/情节铺垫每章累计不超过 40%，宁可拉长性爱场景也不要用情节凑字数。
- **扩写必须真正新增内容**：收到"加长/充实"反馈后用插入法加新段落，不要整文件重写相同文本（空重写陷阱——字节差 <100 会被判定重复调用）。检查手段：写后立刻跑 wordcount 对比。
- **不要急于交付**：写完审阅完再给用户看（高潮次数与叙述一致、章节号对齐、逻辑连贯）。
- 群戏/多人章节（如秘宴、朝堂等大场面）可以是例外，可以有明面多人戏；普通章节按勘察出的玩法路线走。
- 人称笔误高发：插入时"她/他/朕/本王"混用，每章补完通读该章确认人称与逻辑。
